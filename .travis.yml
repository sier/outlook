sudo: required

language: node_js
node_js:
  - "lts/*"
os: osx

before_install:
  - cd Office365

install:
  - sudo npm install -g electron --unsafe-perm=true --allow-root
  - sudo npm install
  - sudo yarn install
  - sudo yarn run dist:macos